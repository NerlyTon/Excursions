<h1>Your Bookings</h1>
<%@bookings.each do |booking|%>
    <h3><%=link_to booking.excursion.title, excursion_booking_path(booking.excursion_id, booking.id)%> for <%=booking.num_of_people%></h3>
    Booked Date:<%=booking.date.strftime("%m-%d-%Y")%><br><br>
<% end %>
       